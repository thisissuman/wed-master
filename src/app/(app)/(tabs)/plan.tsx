import { PlaceholderScreen } from "@/components/ui";

export default function PlanScreen() {
  return (
    <PlaceholderScreen
      description="Events and tasks will live here after their vertical slices are built."
      title="Plan"
    />
  );
}
